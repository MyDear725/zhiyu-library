# 设计验收记录

## 验收基线

- 视觉方向：方案 `02 · Spatial Reading Atrium`
- 参考图：`C:\Users\39006\.codex\generated_images\01a01e46-2e8f-72c3-8ccf-77cc765f5830\exec-806502ef-fe10-4665-9542-1390089b63b0.png`
- 最终实现：`design-qa-assets/home-layout-v2.png`
- 移动端实现：`design-qa-assets/home-layout-mobile-v2.png`
- 视口：桌面 `1916 × 890`、移动端 `390 × 844` CSS px，设备像素比 `1`
- 状态：已登录演示用户，首页激活，无当前座位预约
- 归一化方法：参考图原始尺寸为 `1536 × 1060`；按实现宽度等比缩放至 `1265 px`，再从顶部裁切至 `1265 × 712`，与浏览器可见视口并排比较。

## 对照范围

本轮需要同时进行全视图和重点区域比较。全视图用于验证两项主任务与横向社区入口的空间关系；重点区域用于核对导航、借书与占座主卡、实时座位状态卡、社区入口及交互焦点。跨页面一致性另以以下证据复核：

- 借书：`design-qa-assets/books.png`
- 占座：`design-qa-assets/seats.png`
- 社区：`design-qa-assets/community.png`
- 签退正反馈：`design-qa-assets/checkout-celebration.png`

## 必验表面

| 表面 | 验收结果 |
| --- | --- |
| 字体与层级 | 中文标题、正文、辅助文字的字号和字重已拉开；所有数字使用 Times New Roman；首屏主标题在当前视口内具有明确视觉重心。 |
| 间距与布局 | 首页采用 `借书 / 占座` 双主卡与横向 `社区` 服务条，主次关系清晰；页面边距、卡片内距和栏间距形成统一节奏。 |
| 色彩与令牌 | 深海军蓝、冷白、雾蓝和少量亮蓝组成统一色系；文字、按钮与背景对比清晰。 |
| 图片与资产 | 首页使用专门生成的真实图书馆中庭和书架图；图标来自 Phosphor 图标库导出的静态 SVG，未使用 emoji 或临时占位符。 |
| 文案与内容 | 首页只保留借书、占座、社区三个核心入口；借书推荐说明简洁，座位状态和社区功能均使用任务导向文案。 |
| 图标 | 书籍、座椅、对话、定位和箭头的线宽与色彩一致，装饰图标正确隐藏于辅助技术。 |
| 状态与交互 | 搜索与相关推荐、选书问答入口、座位三状态、场景匹配、预约与签退、匿名聊天、馆内问答均已逐项验证。 |
| 无障碍与响应式 | 交互控件使用语义化按钮和显式标签；键盘焦点以导航下划线光晕显示；提供 `prefers-reduced-motion` 降级，并包含桌面、平板和窄屏断点。 |

## 迭代记录

1. 初版社区栏的标题与链接在小高度视口中靠得过近，严重度 `P2`。通过缩小社区标题比例、将行动入口定位到下方并增加安全间距解决；证据由 `home-implementation-01.png` 更新至 `home-implementation-02.png`。
2. 导航按钮的原生焦点轮廓形成过大的竖向方框，严重度 `P2`。改为与激活态一致的下划线光晕焦点指示，消除突兀方框，同时保留键盘可见性；最终证据为 `home-implementation-final-02.png`。
3. 参考图与实现所使用的中庭照片拍摄角度略有不同，记为 `P3`。该差异来自为实际网页重新生成的高分辨率资产及响应式裁切，不影响版式意图、信息层级或交互，接受保留。
4. `1916 × 890` 桌面视口中，首页原三列比例导致社区栏仅约 `192px`，且三个功能图标分别使用不同纵向定位，记为 `P2`。现已改为带最小宽度约束的非对称三列栅格，将三个图标统一到 `45%` 视觉基线，并将社区文案改为平衡的两行结构。证据为 `home-layout-before.png`、`home-layout-after.png` 和 `home-layout-comparison.png`。
5. 上一轮仍保留三根纵向卡片，功能入口相互争夺焦点，社区在窄栏中难以形成稳定排版，记为 `P2`。本轮改为 `借书 / 占座` 两项主任务并列，社区作为下方横向服务入口；图标、标题、行动入口均进入统一栅格，桌面和移动端均未发现内容重叠或裁切。证据为 `home-layout-v2.png` 与 `home-layout-mobile-v2.png`。

## 功能与稳定性复核

- 注册、登录和会话恢复正常。
- 顶部导航及五个主视图切换正常。
- 搜索《百年孤独》可返回馆藏、相似/相关书目及简短推荐理由。
- 不知道读什么时可进入三问选书流程。
- 楼层地图、空闲/使用中/暂离状态、座位选择、预约、我的页面和签退流程正常。
- 场景问答可根据自习、讨论或问题方向推荐同类用户所在区域。
- 社区补给站、同伴广场匿名开关、馆内 RAG 问答均正常。
- 签退后庆祝卡、积分反馈和纸屑动画正常；减少动态效果偏好下会自动降级。
- 浏览器控制台未发现运行时错误；Lint、测试和生产构建均通过。
- 最终复核未遗留 `P0`、`P1` 或 `P2` 问题。

passed
